services:
  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - app-network

  backend:
    build:
      context: ./backend
      args:
        RUN_TESTS: ${RUN_TESTS:-false} # Defaults to false, can override with env var
    ports:
      - "5000:5000"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:5000
      - DB_HOST=database
      - DB_PORT=3306
      - DB_NAME=CloudCoreDB
      - DB_USER=root
      - DB_PASSWORD=${DB_PASSWORD:-defaultpassword}
      - FileStorage__BasePath=/app/storage
      - Email_Host=mailhog
      - Email_Port=1025
      - Email_SenderEmail=mailhog@test.com
      - Email_Sender=CloudCore
      - JWT_KEY=${JWT_KEY:-your-super-secret-key-that-is-at-least-32-characters-long}
      - JWT_ISSUER=${JWT_ISSUER:-CloudCore}
      - JWT_AUDIENCE=${JWT_AUDIENCE:-CloudCore}
      - JWT_TOKEN_EXPIRATION_DAYS=${JWT_TOKEN_EXPIRATION_DAYS:-7}
    volumes:
      - ./backend/storage:/app/storage
    depends_on:
      database:
        condition: service_healthy
    networks:
      - app-network

  database:
    image: mysql:8.0
    environment:
      MYSQL_HOST: database
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-defaultpassword}
      MYSQL_DATABASE: CloudCoreDB
      MYSQL_CHARACTER_SET_SERVER: utf8mb4
      MYSQL_COLLATION_SERVER: utf8mb4_unicode_ci
    volumes:
      - mysql_data:/var/lib/mysql
      - ./database/init:/docker-entrypoint-initdb.d
    ports:
      - "3307:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10
      interval: 10s
    networks:
      - app-network

  mailhog:
    image: mailhog/mailhog:latest
    container_name: mailhog
    ports:
      - "8025:8025"
      - "1025:1025"
    networks:
      - app-network

volumes:
  mysql_data:

networks:
  app-network:
    driver: bridge
